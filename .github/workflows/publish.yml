name: Publish Package

on:
  # Publish on release creation
  release:
    types: [published]
  # Also publish on pushes to main (for continuous updates)
  push:
    branches: [main]
    paths: ['packages/kookie-blocks/**']

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 10.0.0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'pnpm'

      - name: Ensure npm CLI supports OIDC
        run: npm install -g npm@latest

      - name: Configure npm registry
        run: npm config set registry https://registry.npmjs.org/

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build package
        run: |
          cd packages/kookie-blocks
          pnpm build

      - name: Auto-increment version (if not a release)
        if: github.event_name != 'release'
        run: |
          cd packages/kookie-blocks
          npm version patch --no-git-tag-version

      - name: Get version
        id: version
        run: |
          cd packages/kookie-blocks
          VERSION=$(node -p 'require("./package.json").version')
          echo "Extracted version: $VERSION"
          echo "version=$VERSION" >> $GITHUB_OUTPUT

      - name: Check if version exists on NPM
        id: check_version
        run: |
          cd packages/kookie-blocks
          VERSION="${{ steps.version.outputs.version }}"
          echo "Checking if version $VERSION exists on NPM..."
          if npm view @kushagradhawan/kookie-blocks@$VERSION version 2>/dev/null; then
            echo "Version $VERSION already exists on NPM"
            echo "exists=true" >> $GITHUB_OUTPUT
          else
            echo "Version $VERSION does not exist on NPM - proceeding with publish"
            echo "exists=false" >> $GITHUB_OUTPUT
          fi

      - name: Publish to NPM
        if: steps.check_version.outputs.exists == 'false'
        run: |
          cd packages/kookie-blocks
          npm publish --access public

      - name: Commit version bump
        if: github.event_name != 'release' && steps.check_version.outputs.exists == 'false'
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add packages/kookie-blocks/package.json
          git commit -m "chore: bump version to ${{ steps.version.outputs.version }}" || exit 0
          git push

